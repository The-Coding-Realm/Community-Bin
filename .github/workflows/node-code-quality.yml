name: Node Code Quality
on: [push, pull_request]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v2.1.5
      - run: |
          cd javascript
          npx prettier --check .
